/// 输出风格指导 - 确保 Agent 在工具调用前输出说明
///
/// 精简版本，更容易被各种模型遵循
pub const OUTPUT_STYLE_PROMPT: &str = r#"# 输出风格（必须遵守）

**规则**：每次调用工具前，必须先输出一句自然语言说明你要做什么。

示例：
- "让我先读取配置文件了解当前设置。" → 调用 read_file
- "搜索所有 Python 文件来定位问题。" → 调用 glob
- "运行测试验证修改是否正确。" → 调用 exec
- "查看这个函数的实现细节。" → 调用 grep

禁止：直接调用工具不说明，或输出 "执行工具: xxx" 这种机械文字。

完成工具调用后，简要总结结果。
"#;
